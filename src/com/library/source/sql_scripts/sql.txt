This is just a text file for sql scripts so I won't forget what I do in database. :3

1. You need to create a database for this system.
CREATE DATABASE library_system;

2. Let's create a table named persons to store both members and admins
CREATE TABLE persons (
    first_name VARCHAR(50) NOT NULL,
    last_name VARCHAR(50) NOT NULL,
    cid VARCHAR(12) NOT NULL,
    dob DATE NOT NULL,
    phone VARCHAR(15) NOT NULL,
    PRIMARY KEY (cid));

3. Create a table named accounts to store librarians and members' accounts.
    CREATE TABLE accounts (
        id VARCHAR(7),
        cid VARCHAR(12) NOT NULL,
        password TEXT NOT NULL,
        status SMALLINT NOT NULL,
        PRIMARY KEY (id),
        UNIQUE (cid),
        FOREIGN KEY (cid) REFERENCES persons(cid));

    INSERT INTO accounts VALUES ('MU00001', '219-37-8710', crypt('password', gen_salt('bf')), 1);

4. Create a table named members to store members' expire date.
    CREATE TABLE members (
        id VARCHAR(7),
        expire_date DATE NOT NULL,
        PRIMARY KEY (id),
        FOREIGN KEY (id) REFERENCES accounts(id));

5. Create a table named books to store books


